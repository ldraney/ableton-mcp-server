{
  "mcpServers": {
    "ableton": {
      "command": "poetry",
      "args": ["run", "ableton-mcp"],
      "cwd": ".",
      "description": "Control Ableton Live via OSC. Create tracks, add MIDI clips with notes, control tempo/transport, and manipulate the session view. Requires Ableton Live running with AbletonOSC enabled."
    }
  }
}
